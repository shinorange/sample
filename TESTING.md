# テスト手順書

作業ディレクトリ名: **`minecraft_new`**

## 0. 前提

- **Java 21**(`java -version` で確認。21以外だとビルドできません)
- Git とインターネット接続(初回ビルド時に Minecraft 本体・Fabric・依存ライブラリを自動ダウンロードします)

## 1. 作業ディレクトリの用意

```bash
git clone -b claude/minecraft-realistic-sword-crafting-qp3fpy https://github.com/shinorange/sample.git minecraft_new
cd minecraft_new
```

すでにクローン済みの場合はフォルダ名を変えるだけでも構いません:

```bash
mv sample minecraft_new && cd minecraft_new && git pull
```

## 2. ビルド

```bash
./gradlew build          # Windows は gradlew.bat build
```

- 成功すると `build/libs/swordsmith-1.0.0.jar` が生成されます
- **エラーが出た場合**: ログ全文をコピーしてClaudeに報告してください(マッピング名の齟齬であれば数行の修正で直せます)

## 3. 起動方法

**A. 開発クライアント(推奨・一番手軽)**

```bash
./gradlew runClient
```

**B. 通常のゲーム環境**

1. [Fabric Loader](https://fabricmc.net/use/installer/) 0.16以上を 1.21.1 向けにインストール
2. [Fabric API](https://modrinth.com/mod/fabric-api) 0.102.0+1.21.1 以降を `mods` フォルダへ
3. `build/libs/swordsmith-1.0.0.jar` を `mods` フォルダへ

## 4. スモークテスト(約5分・クリエイティブ)

| # | 手順 | 期待結果 |
|---|---|---|
| 1 | クリエイティブでインベントリを開く | 「剣鍛冶」タブがあり、全19アイテムにテクスチャと日本語名 |
| 2 | 鍛冶炉を設置→木炭を右クリック→火打石と打ち金 | 点火し、炎・煙のパーティクルとパチパチ音 |
| 3 | 炉を素手で右クリック | GUIが開く: 色帯付き温度計・炎ゲージ・送風バー・透かし模様・下部に現在℃ |
| 4 | 手ふいごで炉を数回右クリック | 送風バーが伸び、温度計の火柱が上昇していく |
| 5 | 原鉄を持って炉を右クリック | 炉の上に原鉄が3D表示される(点火中は明るく光る) |
| 6 | 言語を English (US) に切替 | 全アイテム名・メッセージが英語になる |

## 5. 全工程テスト(約30分・サバイバル推奨)

| # | 工程 | 手順 | 期待結果 |
|---|---|---|---|
| 1 | 製錬 | 原鉄を炉に入れ、ふいごを数秒おきに吹いて1150℃以上を維持 | 約30秒で「海綿鉄の塊」に変化(進行はGUIの矢印)。進捗「たたらの火」解除 |
| 2 | 火傷 | 熱い海綿鉄を素手のまま持つ | 継続ダメージ+警告。**やっとこを手に持つと**ダメージ停止(やっとこが消耗) |
| 3 | HUD | 熱した加工品を手に持つ | ホットバー右上に温度ゲージ(720/780/950℃の目盛り) |
| 4 | 鍛造 | 金床に置き、鍛冶ハンマーで右クリック連打 | 720℃未満だと「冷えている」拒否。900〜1150℃で「会心の一打!」+火花増量。8打→鋼片、10打→鋼材、12打→荒打ちの刀身 |
| 5 | 焼き入れ①適温 | 荒打ちを780〜950℃(理想865℃)にして水入り樽へ | 「焼き入れ成功」→焼き入れした刀身。ツールチップに焼き入れの質% |
| 6 | 焼き入れ②過熱 | 別の刀身を1000℃超で樽へ | 「ひび割れた刀身」に(→熱して6打で復活できることも確認) |
| 7 | 焼き入れ③低温 | 700℃程度で樽へ | 「焼きが入らなかった」、刀身はそのまま冷える |
| 8 | 焼き戻し | 焼き入れ刀身を**送風していない**点火中の炉(約300℃)に20秒 | 「焼き戻しした刀身」に。**送風して460℃超にすると荒打ちに逆戻り**することも確認 |
| 9 | 研ぎ | 熱いまま研ぎ台を右クリック→拒否。冷えてから24回 | 「研ぎ上げた刀身」に。途中経過はアクションバー表示 |
| 10 | 柄付け | 金床に置き、柄→鍔の順で試す | 順序違いは「まず鍔を〜」とガイド。鍔→柄→柄頭→ハンマー3打で「鍛造鋼の剣」完成 |
| 11 | クオリティ | 会心多めで1本、720℃ギリギリの打撃だけで1本作る | 攻撃力(6.6〜8.5)と等級(並〜神業)がツールチップで異なる |
| 12 | 進捗 | 進捗画面を開く | 専用背景の「剣鍛冶」タブに9個(刀身を割ると隠し進捗「音で学ぶ」) |
| 13 | 自動化 | 炉の燃料スロットへホッパーで石炭搬入 | 自動補給される |

## 6. マルチプレイ(任意)

```bash
./gradlew runServer      # 初回は run/eula.txt を true にして再実行
```

クライアント2つで接続し、①炉GUIを2人同時に開いて温度計が両方動く、②金床の上の加工品が両者に見える、③片方の鍛造進行がもう片方にも反映される、を確認。

## 7. 問題があったら

- クラッシュ: `run/logs/latest.log`(開発環境)または `logs/latest.log` の全文
- ビルド失敗: `./gradlew build --stacktrace` の出力
- 挙動の違和感: 再現手順(何を持って何をクリックしたか)

をそのままClaudeに貼ってください。
